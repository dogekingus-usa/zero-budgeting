---
import BaseLayout from '../layouts/BaseLayout.astro';
import body from '../bodies/404.html?raw';

const title = "404 — Page Not Found | Zero Budgeting";
const description = "Page not found on Zero Budgeting — browse our full article library instead.";
const canonical = "https://zerobudgeting.com/404";
---

<BaseLayout title={title} description={description} canonical={canonical}>
  <div class="mx-auto max-w-4xl px-4 py-8">
    <Fragment set:html={body} />
  </div>
<script>
  // CWOC 2026-08-06: trailing-slash -> clean URL (GH Pages has no _redirects support; 404 page is served for unknown paths)
  (function () {
    var p = window.location.pathname;
    if (p.length > 1 && p.charAt(p.length - 1) === "/") {
      var clean = p.slice(0, -1);
      window.location.replace(clean + window.location.search + window.location.hash);
    }
  })();
</script>

